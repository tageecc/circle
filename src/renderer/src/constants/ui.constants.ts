/**
 * 渲染进程 UI 配置常量
 *
 * 包含前端 UI 相关的硬编码配置值
 */

// ============================================================================
// Language Service UI 配置
// ============================================================================

export const LANGUAGE_SERVICE_UI = {
  /** 补全请求超时时间（毫秒） */
  COMPLETION_TIMEOUT: 500,

  /** Hover 请求超时时间（毫秒） */
  HOVER_TIMEOUT: 1000,

  /** Hover 缓存有效期（毫秒） */
  HOVER_CACHE_TTL: 5000,

  /** 缓存清理间隔（毫秒） */
  CACHE_CLEANUP_INTERVAL: 10000,

  /** 过期缓存清理阈值（毫秒） */
  CACHE_EXPIRY_THRESHOLD: 10000,

  /** 文件内容更新防抖延迟（毫秒） */
  UPDATE_DEBOUNCE: 300
} as const

// ============================================================================
// UI 交互配置
// ============================================================================

export const UI = {
  /** 防抖延迟（毫秒） */
  DEBOUNCE_DELAY: 300,

  /** 节流延迟（毫秒） */
  THROTTLE_DELAY: 100,

  /** Toast 显示时长（毫秒） */
  TOAST_DURATION: 3000,

  /** 虚拟滚动每页项目数 */
  VIRTUAL_SCROLL_PAGE_SIZE: 50
} as const

// ============================================================================
// 性能配置
// ============================================================================

export const PERFORMANCE = {
  /** 大文件阈值（字节）- 1MB */
  LARGE_FILE_THRESHOLD: 1024 * 1024,

  /** 语法高亮最大文件大小（字节）- 5MB */
  MAX_SYNTAX_HIGHLIGHT_SIZE: 5 * 1024 * 1024
} as const
