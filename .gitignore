node_modules
dist
out

# Apple code signing — never commit (private key lives in .p12)
*.p12
/*.cer
.DS_Store
.eslintcache
*.log*

# AI Tracing 数据库（本地调试数据）
circle-traces.db
circle-traces.db-shm
circle-traces.db-wal

# 代码库向量库（sqlite-vec，通常在 userDataPath 下）
circle-vectors.db

# 日志与请求数据（勿提交）
cursor-request-data.json
